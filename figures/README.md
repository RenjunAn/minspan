# Figures

`src/fig_*.py` renders the paper figures from the CSVs in `src/`; shared style
in `src/paperstyle.py`. Planned: source the CSVs from `results/` via collector
scripts so every figure regenerates from raw benchmark outputs (status in
`REPRODUCE.md`).
